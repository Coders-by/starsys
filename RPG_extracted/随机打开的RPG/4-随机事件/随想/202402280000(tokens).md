从目前各个 AI 模型的介绍和使用来看，基本上围绕着一个关键点：tokens。也即如何将某个内容先拆分成tokens，然后丢给另一个接口去处理。比如所谓的文生视频模型，首先是将用户输入的文本信息丢给某个语言模型，然后这个语言模型去输出一些专门用于视频生成的tokens，接着将这些tokens再喂给视频模型。区分不同的文生视频模型能力的一个重要指标，就是这个承担中间角色的语言模型对tokens的抽象能力以及这些tokens和视频模型的匹配程度。

这里面有一个问题，为什么不直接用原始文本作为视频模型的输入，而非要经过语言模型的中间处理过程呢？